CREATE TABLE IF NOT EXISTS persons(
                        id bigint,
                        uid varchar(500),
                        created timestamp,
                        modified timestamp,
                        name varchar(500)
);